import MeshRenderer from '../../../engine/utils/mesh-renderer';
import { IComponentProps } from '../../common/types';

export interface IProps extends IComponentProps {
	meshRenderer: MeshRenderer;
}
